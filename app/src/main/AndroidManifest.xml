<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- For full screen intents/notifications -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- Fall Detection Permissions -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <application
        android:name=".SilverLinkApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SilverLink"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SilverLink">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.reminder.ReminderAlertActivity"
            android:exported="false"
            android:theme="@style/Theme.SilverLink"
            android:showOnLockScreen="true"
            android:launchMode="singleTop" />

        <activity
            android:name=".ui.onboarding.QrScanPortraitActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.SilverLink" />
        
        <!-- Receiver for Alarm -->
        <receiver android:name=".feature.reminder.AlarmReceiver" 
            android:exported="false"/>

        <!-- FileProvider for camera photos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Fall Detection Activity -->
        <activity
            android:name=".feature.falldetection.FallAlertActivity"
            android:exported="false"
            android:theme="@style/Theme.SilverLink"
            android:showOnLockScreen="true"
            android:launchMode="singleTop" />

        <!-- Fall Detection Service -->
        <service
            android:name=".feature.falldetection.FallDetectionService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Boot Receiver for Fall Detection -->
        <receiver
            android:name=".feature.falldetection.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
        <!-- Family Emergency Polling Service -->
        <service
            android:name=".feature.falldetection.EmergencyPollingService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- Family Emergency Activity -->
        <activity
            android:name=".feature.falldetection.FamilyEmergencyActivity"
            android:exported="false"
            android:theme="@style/Theme.SilverLink"
            android:showOnLockScreen="true"
            android:launchMode="singleTop" />

    </application>

</manifest>

